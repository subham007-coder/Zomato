<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Zomato</title>
</head>
<body>
    
    <!-- <div className="flex mt-0 w-full h-full justify-between items-center text-center">
        <div className="navlogo">
          <img
            className="w-20"
            src="https://b.zmtcdn.com/web_assets/b40b97e677bc7b2ca77c58c61db266fe1603954218.png"
            alt=""
          />
        </div>

        <div className="mx-10 flex text-center items-center py-2 px-4 w-1/2 border-solid shadow-sm rounded-lg locations search">
          <div className="flex text-center items-center locationBox">
            <i className="mx-2 fa-solid fa-location-dot"></i>
            <select className="px-3 py-2 bg-white border border-white placeholder-slate-400 focus:outline-none block w-full rounded-md sm:text-sm" name="" id="">
              <option value="">Kolkata</option>
              <option value="">Detect current location</option>
            </select>
          </div>

          <div className="searchBox w-full flex text-center items-center">
            <p className="px-2">|</p>
            <i className="mx-2 fa-solid fa-magnifying-glass"></i>
            <input
              type="text"
              className="px-3 py-2 bg-white border border-white placeholder-slate-400 focus:outline-none block w-full rounded-md sm:text-sm"
              placeholder="Search for restaurant, cuisine or a dish"
            />
          </div>
        </div>

        <div className="login flex items-center gap-4">
          <button>Log in</button>
          <button>Sign up</button>
        </div>
      </div> -->
<ul>

  <% for( let listing of allListings) { %>
    <li> <a href="/listings/<%= listing._id %>"><%= listing.title %></li></a>
    <% } %>
    
  </ul>


</body>
</html>